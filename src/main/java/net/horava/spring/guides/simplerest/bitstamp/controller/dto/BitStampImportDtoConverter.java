package net.horava.spring.guides.simplerest.bitstamp.controller.dto;

import net.horava.spring.guides.simplerest.bitstamp.entity.BitStampTicker;

/**
 * @author Martin Horava
 */
public class BitStampImportDtoConverter {

//    public static BitStampTicker convert(BitStampTickerImportDto bitStampTickerImportDto) {
//        return new BitStampTicker()
//            .withAsk(bitStampTickerImportDto.getAsk())
//            .withBig(bitStampTickerImportDto.getBid())
//            .withHigh(bitStampTickerImportDto.getHigh())
//            .withLast(bitStampTickerImportDto.getLast())
//            .withLow(bitStampTickerImportDto.getLow())
//            .withOpen(bitStampTickerImportDto.getOpen())
//            .withTimestamp(new )
//    }

    private BitStampImportDtoConverter() {
    }
}
